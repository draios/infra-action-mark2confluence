[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
jinja2 = ">=3.0.3"
pyyaml = ">=6.0"
pytest = ">=7.0.1"
loguru = ">=0.5.3"
supermutes = ">=0.2.5"

[dev-packages]
pytest = ">=7.0.1"

[requires]
python_version = "3.12"

[scripts]
mark2confluence = "python mark2confluence/main.py"
